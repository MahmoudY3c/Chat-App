// eslint-disable-next-line
import { bg } from "../extends";
// eslint-disable-next-line
import colors from "../variables/colors";

export const AppHeader = {
  // ...bg,
  // backgroundColor: colors.default,
  'minHeight': '100vh',
  'display': 'flex',
  'flexDirection': 'column',
  'alignItems': 'center',
  'justifyContent': 'center',
  'fontSize': 'calc(10px + 2vmin)',
  'color': 'white',
}